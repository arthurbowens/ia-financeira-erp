{
  "version": 2,
  "framework": "angular",
  "buildCommand": "npm run build:vercel",
  "outputDirectory": "dist/ia-financeira-erp/browser",
  "installCommand": "git lfs install && git lfs pull && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}